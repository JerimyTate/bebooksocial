version: 0.1
frontend:
  phases:
    # IMPORTANT - Please verify your build commands
    build:
      commands:
        - hugo --minify --gc --destination ../public --source ./ --themesDir ../.. --baseURL https://main.d2td877uut3l4y.amplifyapp.com/
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: public/
    files:
      - '**/*'
  cache:
    paths: []